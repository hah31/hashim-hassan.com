# GitHub Pages runs Jekyll, which would publish these .md files as web pages.
# They're notes for editing the site, not pages, so keep them out of the build.
exclude:
  - content.md
  - images/README.md
